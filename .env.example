PORT=3000
OPENAI_API_KEY=
DATABASE_URL=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Optional: needed only for `npm run provision:demo`, not `npm run dev`.
# Set these in a server-only .env.local; never prefix them with VITE_.
SUPABASE_SERVICE_ROLE_KEY=
DEMO_AUTH_PASSWORD=
